## Development Tips

- These components are used by third party consumers, so keep the components as extensible and configurable as possible.
- Do not use hardcoded strings for labels or other UI text; use props.
