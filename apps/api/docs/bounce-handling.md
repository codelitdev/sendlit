Bounce handling contradicted itself** — the Suppression section promised `status='bounced'` from SMTP rejections, but the pipeline retried _every_ failure 3 times, meaning a dead address would burn retries and land on `failed`. Now specified: nodemailer `responseCode` 5xx → `bounced` + BullMQ `UnrecoverableError` (no retry); transient errors retry; exhausted attempts → `failed`. The pipeline diagram and tests section match.
